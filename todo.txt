CURRENT ISSUES:
- Printing issue: If there are excess courts (eg num courts > total players/4), then schedule printer will put the same team into multiple courts per round; ie, fills all courts every round
    - 4 total players, 12 rounds, 4 courts -> results in fill all courts for 3 rounds then stopping bc that places all 12 games

    
CONCEPTS:
- Should same-gender opponent repeating be weighted differently than cross-gender opponent repeat?
- To let this be flexible to number of teams, instead of Team.Player1 and Team.Player2 it should be Team.Players = [Player1, Player2]
- For even number of games:
    num_games*2 // num_gender = 0, for both men and women
    min(num_games) = LCM(num_men/2, num_women/2); every that many games will be even

TODOS from 3-22-2026 tourney (Joel convo)
- General code cleanup (remove unused/repeat/old code)
- Add names as optional
- Have calculator accept Men No. and Women No, and suggest round number
- On Schedule add Played? checkbox that crosses out round
- Have names included in a POST request, so multiple tourneys at once?
- Give created toureny a key so anyone can go to page/KEY and it will pull up schedule WITH names
- Move each game to its own line (with clear rounds break)
    - Add Score Diff boxes to side of each team when printing
- Create blank tourney doc (essentillay just "_____" for names)

WEBPAGE TODOS:
- Check for schedule print formatting, add print button if needed
X Ask copilot for reformatting page suggestions (feed screenshot)
x provide table of parameters (name/type/default/desc)
x “Play count & balance” quick calculator + explanation (eg for X players on 2 courts, recommend Y rounds for balance)
x add note that if number of men/women is off by more than +-1, may want to fix it another way (eg have a guy play as a woman) bc the numbers will be off; 8 men 4 women means the guys play in half the games, and the women never get an off
x Add link to GitHub repo to the web page
x ensure website gets hit without trailing slash 
x Update GitHub readme


TACTICAL TODO:
- Add "Off" column to schedule per round
- Make schedule printable!
- Verify that if played eg 10.5 rounds for 7=men=women, can just play 10 2-court rounds then drop the second court on the last round and it's optimal (print weighting for last round, confirm there's a standout?)
- Update stats to log num_men and num_women separatly
- Test flat algorithm for Men!=Women
- Create "optimal" for num_men = range(2,11), num_women = num_men +-1, and total points ~= 180 (12 rounds to 15); rounds could be 11, 15, 21, 25, 30, 42 (meaning could be 4 to 16 rounds, realistically)
    - Result would be a grip with / populated; men across side, women across top, and men=women+-1 populated; per grid squad would be RoundsxPoints (eg 12x15pts, or 10.5x15; 0.5 means last round plays 1 game)
- General code cleanup, now that is flattened (eg remove comments and references to cull_num_Courts)
- implement ability to generate fractional number of rounds (eg if 2 courts and ask for 10.5 rounds, generate 10 2-court rounds and 1 1-court round)
- Create webpage to allow internet webpage access; either host python script somewhere, or convert to Javascript and use Github live webpages
x Merge back to dev
x Merge back to Main
x Sweep updated flat algorithm parameters/weights
x Sweep optimized (men=women=range(2,11), num_courts=2, num_rounds=range(4,19)) to see how it comes out
x have PrintStats print GameRng differentiated between men and women
x Current problem: can't get games_range to 0 for num_men=num_women=7 or 9 when num_courts=2 and num_rounds < 17; num_men=women=8 or 10 requires cull to be False
    ONCE CHANGED TO FLAT SINGLE-ALL-TEAM-ROUND, it seems to work!!! Assuming numbers are sensical; eg if num_men=women=7, then games_range=0 for total 7 games, 14 games, 21 games, etc;
        Need half-game for when more than one court, or confirm that just dropping the last game works
x Test functionality for Num Men != Num Women -> works well, added some "None" players for teammaking then cut those players
x Have stats printout also save to CSV, appending stats as well as NumMen/Women, num rounds, and num courts
x Create and implement "weighting coefficients" variables with the other variables, for repeat opponents, repeat teammates, and number of games played
x Create "update stats" function in Player that goes through and calculates num games, num repeat same gender opponents, repeat diff gender opponents, and repeat teammates
x Create Stats function for the schedule that will generate overall stats (min/max/avg)


ALGORITHM OPTIMIZING:
Inputs to the function:
- Num Men, Num Women, Num Rounds, Num Courts
Algorithm handles:
- including num_courts in the cull function vs not
- repeat_exponential of 0, 1, 2, 3
- differening opponent, teammate, and games played weights

Need to review algorithm and trial-and-error different algorithm handles, to get it to best handle all cases:
- Num Men != Num Women
- For all tested scenarios so far (which have always assumed men=women), this seems the best outcome:
    - repeat teammate/opponent weight = 1
    - total game count weight = 100
    - cull_num_courts = True (needed for scheduling, algo treats as False if num_courts=1 or None)
    - repeat_exponential should be 2 just because


IDEAS:
- Make cull_num_courts True unless number of courts is 1 or None? (I guess irrelevant if num_courts is None bc will never cull)
- Try running with eg 3 courts when num of teams only allows for single court, see if it breaks


AI prompting for data analysis:
Please analyze this workbook to give me insights.  The primary KPI is to minimize Games Played Range, then to minimize the maximum Repeat Teammates, then to minimize maximum Repeat Opponents.

The algorithm levers I can use to accomplish this are the different weights.
I would like to find algorithm settings that work great regardless of the number of rounds, number of courts (may be an integer or Unlimited), or number of players.
Therefore, please treat each environment (rounds × courts × players) as a separate “scenario,” pick the lexicographic best setting(s) in each scenario, and then look for weight settings that win across all scenarios.

Analysis so far:
For 4-6 num of each gender and 1-2 courts, with rounds 3-20:
    opp history weights should be non-zero but tiny, with 1 or 100 being close behind
    teammate history weights should be 1, with 100 essentially the same; can't be zero
    games played weight should be 100; as minimizing range of games played across players is primary goal, max this weight
    cull num courts should be false; if the rounds is an "even" number then this is irrelevant, otherwise it helps. Could get dicey scheduling-wise tho, as based on number of teams and courts, could mean a team is assigned to two simultaneous games...
For 2-4 num of each gender and 1 court, with rounds 3-20:
    cull num courts should be false; making True means worst-case GPR will be 2 instead of 1 (although shouldn't it just always be False if courts=1?)
    opp history weight and teammate history weight should be low but non-zero
    games played weight should be higher
For 2-11 num of each gender (equal), 1/2/10/Unlimited courts, 12 rounds:
    and CullNumCourts either True or False (but treated as False if num_courts=1 or unlimited):
        Optimal is high GamePlayedWeight, medium RepeatTeammateWeight, small RepeatOpponentWeight;
        Equivalent for CullNumCourts True/False (likely due to num_courts=1 treating CullNumCourts as False)